<?xml version="1.0" encoding="utf-8"?>
<restrictions xmlns:android="http://schemas.android.com/apk/res/android">

    <restriction
        android:key="proxy"
        android:title="@string/proxyUrl"
        android:restrictionType="string" />

    <restriction
        android:key="alias"
        android:title="@string/proxyAlias"
        android:restrictionType="string" />

    <restriction
        android:key="fixed"
        android:title="@string/isFixed"
        android:restrictionType="bool"
        android:defaultValue="false" />

</restrictions>